import type { GiftRecord, StatsSummary } from "@/lib/types";

// TDD Red Phase: Stub implementation
export function aggregate(records: GiftRecord[], now: number): StatsSummary {
  throw new Error("Not implemented");
}
