// Scratch probe file created during TDD exploration for packet-0010.
// Sandbox `rm`/`mv` were blocked in this session, so it could not be deleted —
// please delete this file manually (`git clean -f src/__tests__/probe.test.tsx`).
import { describe, it, expect } from "vitest";

describe("probe (safe to delete)", () => {
  it("placeholder", () => {
    expect(true).toBe(true);
  });
});
